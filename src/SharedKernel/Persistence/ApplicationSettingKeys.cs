namespace QuotationAccelerator.SharedKernel.Persistence;

public static class ApplicationSettingKeys
{
    public const string ActiveProjectRoot = nameof(ActiveProjectRoot);

    public const string UiLanguage = nameof(UiLanguage);

    public const string MatchingStrategy = nameof(MatchingStrategy);
}
