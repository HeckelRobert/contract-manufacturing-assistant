<UserControl x:Class="QuotationAccelerator.Desktop.Views.InquiryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
  <ScrollViewer Padding="24">
    <StackPanel MaxWidth="720" HorizontalAlignment="Left">
      <TextBlock Text="{Binding Heading}"
                 FontSize="24"
                 FontWeight="SemiBold"
                 Margin="0,0,0,16" />

      <Grid Margin="0,0,0,12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="180" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding QuantityLabel}" VerticalAlignment="Center" Margin="0,8" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Margin="0,8" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding MaterialLabel}" VerticalAlignment="Center" Margin="0,8" />
        <ComboBox Grid.Row="1" Grid.Column="1"
                  ItemsSource="{Binding Materials}"
                  SelectedItem="{Binding Material}"
                  Margin="0,8" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding SurfaceTreatmentLabel}" VerticalAlignment="Center" Margin="0,8" />
        <ComboBox Grid.Row="2" Grid.Column="1"
                  ItemsSource="{Binding SurfaceTreatments}"
                  SelectedItem="{Binding SurfaceTreatment}"
                  Margin="0,8" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding PartDescriptionLabel}" VerticalAlignment="Center" Margin="0,8" />
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding PartDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,8" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="{Binding DrawingLabel}" VerticalAlignment="Center" Margin="0,8" />
        <DockPanel Grid.Row="4" Grid.Column="1" Margin="0,8">
          <Button Content="{Binding SelectPdfButtonText}"
                  Command="{Binding SelectDrawingCommand}"
                  DockPanel.Dock="Right"
                  Margin="8,0,0,0" />
          <TextBox Text="{Binding DrawingFileName, Mode=OneWay}"
                   IsReadOnly="True"
                   ToolTip="{Binding DrawingFileName}" />
        </DockPanel>
      </Grid>

      <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
        <Button Content="{Binding AnalyzeInquiryButtonText}"
                Command="{Binding AnalyzeInquiryCommand}" />
      </StackPanel>

      <TextBlock Text="{Binding StatusMessage}"
                 Margin="0,20,0,0"
                 TextWrapping="Wrap"
                 Foreground="#666666" />
    </StackPanel>
  </ScrollViewer>
</UserControl>
